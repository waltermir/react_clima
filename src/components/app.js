import React, { Component } from 'react';
import SearchBar from '../conteiners/search_bar'
export default class App extends Component {
  render() {
    return (
      <SearchBar></SearchBar>
    );
  }
}
